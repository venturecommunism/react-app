import React from 'react'
import {
  Button
} from 'react-native'

const DateTimePicker = ({taskid, placeholder}) =>
  <Button title={placeholder} onPress={() => alert('not implemented yet for web')} />

export default DateTimePicker
