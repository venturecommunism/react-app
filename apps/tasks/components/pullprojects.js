import React from 'react'

const PlainResultComponent = ({ result }) => (
  <div>
    <h3>Pull Projects </h3>
      <h4>result: {JSON.stringify(result)}</h4>
  </div>
)

export default PlainResultComponent
