import React from 'react'
import {
  ScrollView
} from 'react-native'

import MainComponent from './maincomponent'

const App = () =>
  <ScrollView>
    <MainComponent />
  </ScrollView>

export default App
