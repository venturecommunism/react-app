// import {Account, Asset, Keypair, Memo, Operation, TransactionBuilder} from 'stellar-base'
import Stellar, {FederationServer} from 'stellar-sdk'

export default {
  transactstellar({conn, transact}) {


console.log(Stellar.Asset)
/*
      let transaction = new TransactionBuilder(this.session.getAccount())
          .addOperation(operation)
          .addMemo(memo)
          .build();

        transaction.sign(Keypair.fromSeed(this.session.getSecret()));
*/

  },
}
